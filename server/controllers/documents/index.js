const Show = require('./Show')
const List = require('./List')
const Upload = require('./Upload')
const View = require('./View')
const Delete = require('./Delete')

module.exports = { Show, List, Upload, View, Delete }
