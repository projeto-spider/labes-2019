const Create = require('./Create')
const List = require('./List')
const Update = require('./Update')
const Destroy = require('./Delete')
const Show = require('./Show')

module.exports = { Create, List, Update, Destroy, Show }
